# 学习工作台 — 发给别人怎么跑

这是 Flutter 桌面/手机学习计划应用。数据存在本机，没有云同步。

## 环境（和打包时一致）

- Windows 10/11（下面按 Windows 写）
- Flutter **3.44.8** stable（Dart **3.12.2**）
- 跑 Windows 桌面：安装 Visual Studio，勾选「使用 C++ 的桌面开发」
- 跑安卓：另装 Android Studio / SDK

安装 Flutter：https://docs.flutter.dev/get-started/install/windows

装好后在终端执行：

```text
flutter doctor
flutter config --enable-windows-desktop
```

`flutter doctor` 里 Windows 那一项要是勾。

## 启动

解压后进入项目目录：

```text
cd study_manager
flutter pub get
flutter run -d windows
```

改过代码后如果界面不对，热重启：终端里按大写 `R`，或关掉再 `flutter run -d windows`。

手机：

```text
flutter run -d android
```

Windows 编插件若提示 symlink，打开系统「开发人员模式」后再编。

## 依赖（已写在 pubspec.yaml）

- shared_preferences
- url_launcher
- crypto
- flutter_localizations

不用配 API Key，不用数据库。

## 发给对方时注意

- 压缩包里没有 `build/`、`.dart_tool/`，对方必须自己 `flutter pub get`
- 对方电脑上的打卡/目标是空的，不会带上你本机数据
- 登录是本机账号，对方要自己注册
- 「打开不背单词」需要对方手机已安装该 App
